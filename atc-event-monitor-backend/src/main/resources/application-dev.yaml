spring:
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://mongo:27017/atcdb}

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}

    consumer:
      group-id: atc-group
      auto-offset-reset: earliest