apiVersion: v1
kind: Service
metadata:
  name: atc-backend-service
  labels:
    app: atc-backend

spec:
  selector:
    app: atc-backend

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

  type: ClusterIP